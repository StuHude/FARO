# FARO continuation status: 2026-08-28

## Active screens

All jobs use `ailab-dnacoding`, every positive tag in `rjob_tags.txt`, and
SAMTok-only initialization. Each configuration is locked to `egfepo_train_5120`
(5,120 rows, 2,560 no-target rows) and 10 outer steps.

- R21 `dna-fepo-native-rank-local-10step-2g-73872300`
- R22 `dna-fepo-scale-stratified-native-rank-local-6039d`
- R23 `dna-fepo-bidirectional-coarse-fine-10step-2g-b5466`
- R24 `dna-fepo-anchor-kl-10step-2g-1787892498-99977985`
- R25 `dna-fepo-uncertainty-native-rank-local-10ste-3215c`
- R26 `dna-fepo-conservative-null-tail-10step-2g-30508209`
- R27 `dna-fepo-confidence-gated-native-rank-local-554e6`
- R28 `dna-fepo-margin-calibrated-native-rank-local-c4628`

At the latest poll, all eight are `Inqueue` with `STARTING` replicas and no
assigned node. Scheduler events report gang `Unschedulable`; no checkpoint or
`metrics.json` has been produced. Do not cancel these training screens solely
for queue time. The 8 -> 6 -> 4 -> 2 -> 1 fallback applies to eval jobs.

## R25 hypothesis

R25 retains R18 native-relative first-divergence geometry credit. A detached
confidence factor from calibrated per-prefix entropy plus missing top-support
mass rescales only trajectories that improve both cIoU and boundary IoU. The
fixed confidence floor is `0.25`; no teacher, OPD target, counterfactual label,
extra expert/router, PixVL weight, or self-supervised loop is used.

## Next action

When a candidate writes a finished `metrics.json` and adapter, run the full
512-row adaptive holdout eval with 20,000 paired bootstrap resamples. Apply its
preregistered gate before any official RefCOCO/GRefCOCO transfer evaluation.

## Continuation audit (2026-08-28 13:45 HKT)

The eight screen jobs were re-polled with network access and remain
`Inqueue/STARTING` with zero active replicas; no candidate checkpoint or
metrics file exists yet. The local monitor is running in the foreground
session `24081` (60-second interval) and will submit only completed candidates
to the adaptive evaluator. The login node's rjob DNS endpoint was intermittently
unreachable, but direct escalated queries succeeded.

The shared training submit wrapper now accepts either
`processor_config.json` or the actual SAMTok `preprocessor_config.json` during
local model preflight. Worker-side loading and the contract validator remain
authoritative. No files were written under `PixVL_ailab`; disk usage under this
project is approximately 25G, below the 700G cap.

The paired holdout analyzer now accepts explicit CI-lower-bound gates via
`--min-utility-ci-lower` and `--min-positive-ciou-ci-lower`; these must be used
for R22-R25 instead of relying on the mean-only legacy gate. The R22 geometry
registry was also corrected so legacy non-area schedules do not read absent
`area_stratum` fields.
